- Major features:
[ ] - On screen color picker
[ ] - All the features that are possible to be set in settings
      are to be set via commands and optional shortcuts. This way 
      the user might open up a text edit box and input a command like 
      "Draw" or "Erase" or "Ruler" or "Clear Screen".
[ ] - After you have the shortcuts and commands working, add a way fro the user
      to create typedef for commands, this way it might be easier for the user 
      to have their own names. This shoud be easy and not take much. 
      This all might be stored in a big settings file, json shoud be fine.

- Minor thing before to do before release:
[ ] - Structure the code for every cpp<->h file
[ ] - Redo core base, right now its all in random order
[ ] - Text drop frame rate from more than 10k to 1k, 
      what a great pipiline i have here !!!
[ ] - Look into cactus image viewer resource file and see why he has so much stuff there and for what.
[ ] - Fix up the arena code to use less memory and cpu %, might even not run this that much when you have it not updating anything 
      or some like that

- Current feature working progress:
[ ] - Have a utility to add new shortcuts for later when you will have ui for shortcut recordings
[ ] - Prepare the codebase to work from both the debug build path and the release dir structure
[ ] - Have the app fail with win32's MessageBox if something fails to get initialised
[ ] - Add the min version for os at init time
[ ] - Have the widnow be maximized. 
      When we have taskbars that hide we get a full screen texture for drawing.
      What you shoud do is this, just have it be what ever the size of maximized is.
      Then have a command for the user that will reset the app, this will redo all the stuff in the app
      with all the textures and their sizes.
      Then you might also just have a toggle for the app to do that automatically
      when the size of maximized gets changed mid runtime dur to some
      weird task bar position change.
[ ] - The first ever run of the app the user shoud set their own shortcut via some set up ui.
      That shortcut will be the main shortcut to turn on the app.
      There might be the case where the user forgets that the app is running, in that case they will run it again,
      you should have the app get active and show the command line for the user to interact, this might be 
      really nice for the users that dont have a good handle on the app and the flow of it yet.

- Stuff to fix or add next:
[ ] - do we need the d3d context to switch for every batch or would it be fine for it to only switch the setting
      that is differnt, might be faster, google and test this when you add a profiler in.

- Minor fixes and additions:
[ ] - The next project shoud be in C. 
      I like this syntax 
      #define dr_rect(...) dr_draw_rect((Rect_Settings){ \
        .softness      = 1.f,                          \
        .corner_radius = 4.f,                          \
        __VA_ARGS__                                     \
    })
    Its called -> "Designated initializer"
      
[ ] - Look into this https://web.archive.org/web/20190913214618/http://www.pjblewis.com/site/posts/2016-04-27-p2-efficient-alpha-blended-windows-directcomposition.html
[ ] - https://github.com/mmozeiko/c_d2d_dwrite for DWrite,
      found this in Digital Grove 
[ ] - Start using wide chars for win32 stuff and not regular asci

- Other projects to learn from:
[ ] - WCAP
[ ] - RadDBG
[ ] - DOOM + DOOM black book

- Some notes:
* -- The Alt/F10 logic works like this:
      Alt or F10 — enters "menu mode", focusing the menu bar and highlighting the first item. The app is now in a keyboard navigation state.
      Arrow keys — left/right moves between top-level items, down opens the dropdown for the focused item.
      Enter — activates the focused item.
      Escape — steps back one level (closes dropdown → back to menu bar → exits menu mode).
      Alt+letter — jumps directly to a menu item by its underlined letter (called an accelerator or mnemonic), e.g. Alt+F opens "File". These underlines are hidden by default in modern Windows and only appear once you press Alt.
      The way Windows handles this under the hood is through a WM_SYSKEYDOWN message for Alt (rather than WM_KEYDOWN), and the default window procedure (DefWindowProc) implements all of this menu bar navigation logic for you automatically if you have a menu attached to the window. F10 specifically sends a WM_SYSKEYDOWN with VK_F10, which DefWindowProc also handles.
      --
      So in practice if you're building a standard Win32 window with a HMENU attached, you get all of this for free without writing any of the navigation logic yourself.












