- Major features:
[ ] - On screen color picker
[ ] - All the features that are possible to be set in settings
      are to be set via commands and optional shortcuts. This way 
      the user might open up a text edit box and input a command like 
      "Draw" or "Erase" or "Ruler" or "Clear Screen" 

- Current feature working progress:
[ ] - Have the window be full screen. 
[ ] - Have the app work when the app window gets resized, i guess just redo the pencil state then.
[ ] - Try the app a bit and if it works then start making the ui for it.
      [ ] - Command input field.
            [ ] - Text edit box
            [ ] - Box with possible commands
            [ ] - Have the commands that have shortcuts assigned to them show the shortcuts like in vscode 
            [ ] - Finish with shortcuts
                  [ ] - You cant have 2 shortcuts to the same thing at the same time, have to unbound the older one
                  [ ] - Check for other bug cases, just have the shortcuts be working fine so we can forget about them 
                        and focus on finish up other things.
                        
- Stuff to fix or add next:
[ ] - extend shortcuts to optionally have more than 1 mod (mod + mod + key)
[ ] - start having sequences (todo: first look into them again, there is quite a lot of defails about them)
[ ] - do we need the d3d context to switch for every batch or would it be fine for it to only switch the setting
      that is differnt, might be faster, google and test this when you add a profiler in.

- Minor fixes and additions:
[ ] - Have a way to create a window inside the win32.h/.cpp,
      to then be able to create them inside the test_main and
      test stuff. Right now i cant, since my window is not 
      a part of the os layer.
[ ] - The next project shoud be in C. 
      I like this syntax 
      #define dr_rect(...) dr_draw_rect((Rect_Settings){ \
        .softness      = 1.f,                          \
        .corner_radius = 4.f,                          \
        __VA_ARGS__                                     \
    })
    Its called -> "Designated initializer"
      
[ ] - When drawing have a pen like in cactus image viewer

[ ] - Display the fps on the screen to have it be like in raylib  
[ ] - Look into this https://web.archive.org/web/20190913214618/http://www.pjblewis.com/site/posts/2016-04-27-p2-efficient-alpha-blended-windows-directcomposition.html
[ ] - Start using V memory instead of malloc
[ ] - https://github.com/mmozeiko/c_d2d_dwrite for DWrite,
      found this in Digital Grove 
[ ] - Start using wide chars for win32 stuff and not regular asci
[ ] - Structure the code for core base 

- Some quick ideas:
[ ] - The issue with the rounded corners and then their children poking out might be fixed with a little scissor, that all,
      but that would require the gpu to switch contexts, since we would need to set a different scissor rect per each.













